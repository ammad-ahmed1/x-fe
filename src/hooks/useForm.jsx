import React from "react";

const useForm = ({ formData, validationSchema }) => {
  const handleReset = () => {};
  const handleChange = () => {};
  const handleValidate = () => {};
  const handleSubmit = () => {};
  return { handleReset, handleChange, handleValidate, handleSubmit };
  return <div></div>;
};

export default useForm;
